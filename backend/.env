# Server settings
HOST=0.0.0.0
# PORT=3005
# by default, Uvicorn (FastAPI) runs on port 8000 unless you explicitly tell it to use another port (with --port 3005 or similar).

# CORS - comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Debug mode
DEBUG=true 